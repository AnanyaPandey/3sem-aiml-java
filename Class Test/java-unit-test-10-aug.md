# Java Unit Test 

### 10-Aug-2026

**Q1.** List any four features of Java with one line explanation ? What are the 3 Editions of Java. (2 marks)

**Q2.** What are the Three components of Java Architecture, explain in 1 line each of what each component do.  (3 marks)

**Q3.** Predict the output (3 Marks)

```java
int a = 10, b = 20;
if (a > b) {
    System.out.println("A is greater");
} else if (a == b) {
    System.out.println("Equal");
} else {
    System.out.println("B is greater");
}
```

**Q4.** Predict the output: (2 marks)

```java
String s = "Java Programming";
System.out.println(s.length());
System.out.println(s.toUpperCase());
System.out.println(s.charAt(5));
```

**Q5.** Predict the output: (3 Marks)

```java
        int a = 22;
        int b = 33;

        System.out.printf("Number: a = %d, and b = %d",a,b);

        a = a + b;   // a now holds sum of both
        b = a - b;   // b becomes original a
        a = a - b;   // a becomes original b

  		System.out.printf("Number: a = %d, and b = %d",b,a);
```

**Q6.** Write a Java program to check whether a number entered by the user is odd or even. (3 marks)

**Q7.** What is the difference between `=` and `==` in Java? What operators are these? (1 Marks)

**Q8.** Write a Java program to print the multiplication table of  7.  for the first 10 numbers. e.g.  7 x 10 =  70 (3 mark)